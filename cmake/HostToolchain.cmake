# Optional host-toolchain hook.
#
# The project builds with the default C compiler on the host. This file exists
# so a future CI job can pin MinGW, clang, or another compiler without changing
# the root CMakeLists.txt.

